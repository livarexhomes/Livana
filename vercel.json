{
  "buildCommand": "pnpm run typecheck && pnpm -r --filter '!@workspace/mockup-sandbox' --if-present run build",
  "outputDirectory": "artifacts/property-manager/dist/public",
  "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
}
