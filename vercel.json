{
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/property-manager run build && rm -rf vercel-out && mkdir -p vercel-out && cp -R artifacts/property-manager/dist/. vercel-out/",
  "outputDirectory": "vercel-out",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
