# Taste
- Wants an AI assistant's claims to be backed by real actions: if the bot says it booked/scheduled something, it must actually write to the database via a real tool call — text-only role-play of completed actions is a bug. Confidence: 0.8
- Wants the bot to have complete, accurate company knowledge in its system prompt — mined from real company sources (site pages, docs), not generic or stale content. Confidence: 0.8
- Wants the bot to "handle responses well": graceful, fallback-able handling of upstream AI provider failures (non-JSON/HTML/WAF pages with 2xx status) instead of cryptic JSON.parse crashes or 500s. Confidence: 0.8
- Wants integrations fully linked to the real backend (e.g., Supabase): committed schema/migrations for every table the code depends on — in-memory fallbacks that silently lose data on restart are not acceptable. When the live DB is missing a table (user pasted `relation "projects" does not exist` from the Supabase SQL editor), the fix is a committed, self-contained, idempotent migration file (table + RLS + realtime + storage policies, safe to re-run) that the user runs themselves in the Supabase editor. The user also proactively asks "is there any sql needed" after feature work — expecting the assistant to check whether new code touches tables with no committed DDL and to create the missing migration (plus update the db README) without being prompted further. Confidence: 1.0
- Wants user-facing surfaces (e.g., the website chat widget) upgraded to a modern, polished UI rather than left as dated hand-styled components. Confidence: 0.5
- Expects UI work to be mobile responsive as a first-class requirement — mobile layout (bottom-sheet/landscape behavior), safe-area insets for notched devices, adequate touch targets, mobile-specific input handling (e.g., not disabling pinch-zoom, 16px inputs to avoid iOS focus-zoom), and admin dashboards where cards resize, stats/filters wrap, search scales, and tables never cause horizontal page scrolling on phones. Confidence: 0.8
- Prefers the assistant to work from the actual live repo (points to GitHub URL) rather than relying on pasted transcript fragments. Confidence: 0.6
- Wants root-cause "why" explanations, not just surface fixes: when a symptom is reported (e.g., the bot replying "no listings available"), expects the cause to be traced through the actual code — comparing the failing query against the working app's query and the real schema/source of truth (e.g., the form that writes the data) — and explained clearly. Confidence: 0.6
- Prefers integrations to fail honestly rather than fabricate: no invented fallback listings to make the bot look functional — return empty results so the assistant reports truthfully. Confidence: 0.6
- Prefers loud, actionable diagnostics over silent failures: when fetched data doesn't match expectations (e.g., zero rows pass the status allow-list), log the actual values in use and suggest the concrete fix (e.g., the env var to override) instead of silently returning empty results. Confidence: 0.6
- Wants the admin to be able to actually communicate with website visitors through the chat box — a chat request should become a real two-way conversation (admin replies reach the visitor, visitor can keep messaging), not just a one-shot submission the admin can only flip status on. Confidence: 0.8
- Wants explicit read/unread semantics for incoming chat requests: a received request must not count as "read" by default or by status — it is marked read only when the admin actually opens/replies, unread badges track that flag, and a new visitor message re-flags it unread. Confidence: 0.7
- Prefers consolidating similar admin sections into a single unified view rather than proliferating tabs — explicitly asked to merge the Enquiries and Chat Requests tabs into one Inbox because "the tabs are getting much," with type/status filters replacing separate tabs. Confidence: 0.8
- Wants UI filter chip/badge counts to be consistent and truthful across rows: each count must reflect exactly what would be visible if that chip were clicked (honoring whichever other filter is active), never raw totals that disagree with the displayed list — reported as a bug when two "All" chips showed different numbers. Confidence: 0.7
- Prefers controlled numeric form inputs to render blank when the value is 0 rather than a literal "0" (so typing starts cleanly instead of appending to a visible zero), and to select-all on focus so typing overwrites existing values — a deliberate UX pattern applied consistently to every numeric field via a shared helper. Confidence: 0.6
- Wants user-facing actions to surface errors visibly (e.g., a rendered toast with the real error message) rather than failing silently — an action that stops with no on-screen feedback reads as "not working" (reported Create Project failure traced back to a toast state that was set but never rendered). Confidence: 0.5
- Prefers SQL migrations to be idempotent, self-contained, and convention-matching (mirrors existing repo migration style such as `chat_inquiries.sql`: `CREATE TABLE IF NOT EXISTS`, `DROP POLICY IF EXISTS`/recreate, realtime publication guard) so the user can run them safely in the Supabase SQL editor and re-run them. Confidence: 0.5
- Prefers to hand-review the full migration file (read it end-to-end) as a substitute for executing SQL when no Postgres is available locally, rather than shipping unverified SQL. Confidence: 0.5
- Prefers to keep harmless defensive/resilience code (e.g., the `map_link` retry-on-error in `handleSave`) even after the root cause is fixed, treating it as a safety net against partially-migrated databases. Confidence: 0.4
- Prefers business rules and configuration values (e.g., fee percentages) to be admin-configurable settings — never hardcoded — with a single source of truth that automatically applies everywhere when changed. Confidence: 0.8
- Prefers monetary inputs to use one shared, consistent currency component app-wide: Nigerian Naira formatting with comma separators while typing, leading-zero stripping (a default "0" disappears as soon as the user types), and a ₦ prefix — applied to every money field, including future ones. Confidence: 0.8
- Prefers derived financial values (fees, totals) to be calculated automatically and live in forms rather than manually entered or stored — e.g., Agency Fee = rent × configured %, Total Payable updates as you type, and optional fees appear in the total only when they have a value. Confidence: 0.7
- Prefers hiding technical/internal fields (e.g., latitude/longitude) from users entirely — coordinates should be derived automatically in the background from user-friendly inputs like address search, pasted map links, or current-location, and saved without ever exposing them in the form. Confidence: 0.7
- Prefers removing UI and logic for roles the platform doesn't support (e.g., Agents) rather than leaving stale role-specific copy or features behind when only Admin/Landlord/Tenant exist. Confidence: 0.6
- Prefers compact, minimal, modern SaaS-dashboard styling (explicitly citing Linear/Vercel/Stripe/Notion as the aesthetic): rounded corners, subtle borders, soft shadows, consistent border radii, tight spacing with no excessive padding/whitespace or oversized sections, efficient space use, and clear typographic hierarchy (small uppercase section labels + bold titles). Confidence: 0.9
- Prefers filter/status controls as compact pills with count badges and consistent height on a single responsive row — staying on one line (horizontal scroll with hidden scrollbar on narrow widths) rather than wrapping awkwardly, with the active filter highlighted in the primary color and inactive ones subtle. Explicitly asks for "all status filters on one responsive row whenever possible" with compact, consistently sized pills. Confidence: 0.9
- Prefers toolbar controls (search inputs, sort dropdowns, filter pills) to share the same height and border radius and align perfectly on a single horizontal line — search called out to match the filters' height/alignment and also the notification button's height. Confidence: 0.8
- Prefers summary/stat cards as compact horizontal layouts (small icon on the left, value + label beside it) with reduced height (~30–40% shorter), equal spacing between cards, and consistent rounded corners + subtle shadows — values should be scannable at a glance without tall vertical cards. Confidence: 0.8
- Prefers a clear typographic hierarchy inside list/card items: the primary identity (name) most prominent, the status badge smaller but noticeable, and secondary metadata (location, phone, join date) in lighter, smaller type; property counts compact but visible. Confidence: 0.6
- Prefers compact, elegant empty states (small icon plus a short one/two-line hint) over oversized empty areas. Confidence: 0.6
- Prefers UI redesigns to preserve all existing functionality — explicitly requires "keeping all existing functionality intact" / "without changing any existing functionality" / "do not remove any existing features—only improve the layout, spacing, responsiveness, and visual polish": redesigns may only improve layout, spacing, typography, and polish, never drop or alter features/actions. Confidence: 0.8
- Prefers card/list actions as a compact cluster on the right side of a single-row card (consistent small button heights, tight padding, grouped together) rather than a separate full-width bottom action bar — "reserve the right side for actions only," avoid large empty areas, keep card height low, and let future actions (View/Edit) slot into the same cluster without growing the card. Confidence: 0.7
- Wants one cohesive design system applied consistently across ALL admin pages — identical border radii, uniform card heights, matching button styles, consistent filter pills, unified search components, similar shadows — each page must follow the same language as the already-redesigned ones (Dashboard, Landlords, Users, Properties, Support Queue). This extends to sibling cards on the same page: when one queue card is redesigned (e.g., Support Tickets), the other (Inbox/Enquiries & chat) must get the same compact treatment too — the user explicitly called out that the other card also needed fixing. Confidence: 0.9
- Wants UI redesigns to avoid unnecessary re-renders and keep logic untouched — style/className-only changes with no functional or performance impact, verified by a clean build. Confidence: 0.6
branded, mobile-responsive HTML template — logo, brand colors, professional typography, visually appealing buttons, and a proper company footer — consistently across every outgoing email. Confidence: 0.7
- Wants no dead or unfinished functionality in the UI: every editable setting must actually take effect ("no settings that appear editable but have no effect"), and features that are not fully implemented (e.g., incomplete 2FA) should be hidden or disabled with a clear "coming soon" label rather than displayed as if functional. Confidence: 0.8
- Wants every user-generated support/contact/chat message to create an admin notification that appears in the notification bell and in the admin support dashboard immediately (realtime), so no inbound message goes unnoticed. Confidence: 0.7
- Wants property image galleries to follow modern marketplace conventions (explicitly "similar to Airbnb or modern real estate websites"): clicking the main image opens a full-screen lightbox with prev/next buttons, swipe on mobile, keyboard arrow navigation on desktop, thumbnails, and an easy close. Confidence: 0.6
- Prefers internal-only flags/attributes (e.g., Featured, Verified Landlord) to stay hidden from public pages even when they remain tracked internally — public property pages should show only availability badges (e.g., Available for Rent / for Sale). Confidence: 0.5
- Prefers a downloadable report export (Excel/CSV, optionally PDF) over a scheduled email-digest toggle — e.g., replacing the Weekly Summary Reports toggle with a "Download Report" button that exports platform statistics. Confidence: 0.5
- Prefers mobile navigation to encourage account creation with a single "Get Started" CTA linking to sign-up instead of showing multiple redundant Sign In options. Confidence: 0.5
- Prefers ruthlessly cutting redundant UI text: fewer headings/labels, single-line headers with inline counts (e.g., "Inbox (2)") instead of stacked heading blocks, and removing anything that competes with the essential controls (filters + total count) — "nothing else should compete for attention." Confidence: 0.8
- Prefers multiple filter groups on a page to be visually distinct via tiny uppercase labels (e.g., "Type"/"Status") in fixed-width columns with uniform compact pill sizing and per-group active colors (e.g., blue primary for one group, slate-900 for the other), rather than large headings or visually identical groups. Confidence: 0.7
- Prefers to hand the AI builder structured, professional written specs for UI work (section structure varies per spec — e.g., Page, Objective, Current Issues, Improvements, Goal) rather than terse verbal directions. Confidence: 0.5

ective, Current Issues, Improvements, Goal) rather than terse verbal directions. Confidence: 0.4

 tiny uppercase labels (e.g., "Type"/"Status") in fixed-width columns with uniform compact pill sizing and per-group active colors (e.g., blue primary for one group, slate-900 for the other), rather than large headings or visually identical groups. Confidence: 0.7
- Prefers to hand the AI builder structured, professional written specs for UI work (Page, Objective, Current Issues, Improvements, Goal) rather than terse verbal directions. Confidence: 0.4

