# Livarex Launch-Readiness Fix Prompt (Round 4 — Updated)

## Status check (as of this audit)
- Homepage (`/`) fake stats, footer disclaimer, and the old unreachable "Just Listed" card: still fixed, holding.
- **Regression on homepage:** the Lagos and Ogun city cards under "Explore by City" now both link to the generic `/listings?type=rent` instead of their dedicated pages `/properties-in/lagos` and `/properties-in/ogun`. Restore those links.
- `/about`, `/listings`, and `/blog` are UNCHANGED from the previous audit — none of the fixes below have landed yet. Treat items 0-2 as still fully open.

---
# Livarex Launch-Readiness Fix Prompt (Round 3 — Updated)

Use this with Claude Code (or paste into chat with repo access) to finish fixing the site before end-of-month launch.

Status: homepage stats, city-card links, and the homepage's "Just Listed" mismatch have been fixed. The `/blog` stale-cache issue flagged last round is now CONFIRMED and is the top priority.

---

I'm launching livarex.com.ng at end of this month. Here's what's already fixed and what's still open — work through the open items in priority order.

## ✅ Already fixed (verify these hold, don't regress)
- Homepage no longer shows the old fabricated "12+ / 307+" stat blocks — replaced with honest "Now onboarding — Verified landlords across Lagos & Ogun" copy.
- Footer legal disclaimer added (verification/inspections are informational only, not guarantees; Livarex not a broker unless stated).
- City cards on homepage now link to real city routes (`/properties-in/lagos`, `/properties-in/ogun`) instead of a generic filter.
- The "Just Listed — 3 Bed Lekki" card that was teased on the homepage but unreachable in `/listings` has been removed from the live homepage.

## Regression: restore city-specific links on homepage
The Lagos and Ogun cards under "Explore by City" on the homepage currently both link to `/listings?type=rent` instead of `/properties-in/lagos` and `/properties-in/ogun` respectively. Restore the city-specific links so each card routes to its own city page.

## 0. Fix the confirmed stale `/blog` cache (HIGHEST PRIORITY — regression risk)
Verified: `/blog` is currently serving a fully stale build of the OLD homepage — including the old "12+ / 307+" stat blocks, the old "Just Listed — 3 Bed Lekki" card, and old CTA copy ("Join thousands of happy tenants in Lagos"), none of which exist on the current live homepage anymore. This means every fix made to the homepage so far has NOT propagated to `/blog`.

This is more urgent than it looks: it's not just a missing blog page, it's actively re-exposing the fake stats and unreachable listing you already fixed, to anyone who clicks "Blog" in the footer.

Action:
- Check whether `/blog` is a real route at all, or just falling through to a cached/old build of `/`.
- Clear CDN/edge cache and force a redeploy; confirm `/blog` now either 404s cleanly, shows a "coming soon" page, or renders a minimal real blog list — never a stale homepage snapshot.
- After fixing, spot-check 2-3 other secondary routes (e.g. `/terms`, `/privacy-policy`, `/register`) to confirm they aren't also serving stale builds.

## 1. Sync the About page to match the honest homepage (HIGH PRIORITY)
`/about` still shows hardcoded, inflated numbers that contradict the rest of the site:
- "2,400+ Active Listings"
- "850+ Verified Landlords"
- "36 Cities Covered"
- "4.9/5 Rating"

These don't match the homepage's new honest framing or the actual `/listings` page (0 properties). Fix:
- Pull these from the same live-data source as the homepage stats (or remove/reframe to match the "now onboarding, Lagos & Ogun" language).
- "36 Cities Covered" is especially wrong — only Lagos and Ogun have live city pages. Update to reflect actual coverage.
- If there's no real average-rating data yet, remove "4.9/5 Rating" rather than showing an unsupported number.

## 2. Get real inventory into `/listings`
`/listings` still returns "0 properties found." The homepage no longer overpromises a specific listing, which is good, but the core browse flow is still empty. Before launch:
- Seed a real, verified batch of listings (confirm target number with the team), or
- Add a proper empty state with a working "request a property" flow instead of a bare "0 properties found," if inventory won't be ready in time.

## 3. Audit testimonials for authenticity
Still unchanged: three testimonials (Adebayo O., Chidinma E., Emeka N.) with no verifiable source. Confirm these are real (consent on file) or replace/remove before launch — fabricated testimonials are a false-advertising risk under Nigerian consumer protection rules, not just a trust issue.

## 4. QA pass on "Soon" features
Confirm "Buy" and "Commercial" nav items don't lead to broken/empty routes if clicked — should be disabled or route to a proper "coming soon" page.

---

**Acceptance criteria before launch:** every number, testimonial, and listing visible on the live site must be traceable to real data or explicitly framed as early-access/in-progress — and every route must be serving the current build, not a stale cache. Do a full click-through of every nav link and footer link right before go-live to confirm no stale pages remain.