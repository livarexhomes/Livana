{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile -y"
  },
  "deploy": {
    "startCommand": "node index.js"
  },
  "env": {
    "NIXPACKS_NODE_VERSION": "20"
  }
}