{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g npm@10 && npm ci --no-audit --no-fund"
  },
  "deploy": {
    "startCommand": "node index.js"
  },
  "env": {
    "NIXPACKS_NODE_VERSION": "20"
  }
}